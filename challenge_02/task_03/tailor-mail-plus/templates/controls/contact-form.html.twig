<div id="tailor-mail-plus-form-container-{{ contact_form_id }}" class="tailor-mail-plus-form-container">
    {{ include('common/alert.html.twig') }}
    <form name="{{ name }}" id="{{ id }}" method="post" action="{{ form_action_url | raw }}">
        <input type="hidden" name="action" value="{{ form_action }}">
        <input type="hidden" name="contact_form_id" value="{{ contact_form_id }}">
        {{ nonce | raw }}

        {{ content | raw }}
    </form>
</div>